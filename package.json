{
  "name": "dsh-plugin-collection",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSeek Harness plugin collection",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Xs1KVerOA/dsh-plugin.git"
  },
  "workspaces": [
    "resource-center-plugin",
    "dsh-security"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "npm run check --workspace=dsh-resource-center && npm run check --workspace=dsh-security",
    "test": "npm test --workspace=dsh-resource-center && npm test --workspace=dsh-security",
    "pack": "npm pack --workspace=dsh-resource-center && npm pack --workspace=dsh-security"
  }
}
